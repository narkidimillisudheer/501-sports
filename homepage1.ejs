<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sports Scheduler</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <style>
        body {
            padding-top: 60px; 
        }
        center {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 80vh; 
        }

        
        form {
            max-width: 400px; 
            margin: auto;
        }

        form input {
            width: calc(100% - 12px);
            padding: 8px;
            margin-bottom: 10px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        form select{
            width: calc(100% - 12px);
            padding: 8px;
            margin-bottom: 10px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        form label {
            display: block;
            margin-bottom: 5px;
        }

        form button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }

        form button:hover {
            background-color: #45a049;
        }
    </style>
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Sports Scheduler</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <!-- Add the following li to display the username -->
                    <li class="nav-item">
                        <span class="nav-link" id="usernamePlaceholder">Welcome, <span id="username"></span></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/check">Create Session</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/sportsSessions">All Matches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/mySessions">My Matches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/joined-sports">My upcoming Matches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/cancelled-sports">Cancelled Matches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signout">Sign Out</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container mt-5">
        <h1>Welcome to the Sports Scheduler</h1>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
            <% for(var i=0; i<messages.error.length; i++) { %>
              <div class="alert alert-success">
                     <%= messages.error[i] %>
                  </div>
            <% } %>
          <% } %>
        <center>
            <!-- <% if(messages.length>0){ %>
                <div class="alert aert-success">
                    <%= messages %>
                </div>
                <% } %> -->
                
            <form action="/create-session" method="post">
      <label for="sport">Select Sport:</label>
      <select name="sportsName" id="sport">
        <option value="football">Football</option>
        <option value="basketball">Basketball</option>
        <option value="cricket">cricket</option>
        <option value="Tennis">Tennis</option>
        <option value="Badminton">Badminton</option>
        <!-- Add more options for other sports -->
      </select>
  
      <label for="team1">Team 1 Players (comma-separated):</label>
                <input type="text" name="team1Players" id="team1" class="form-control" required>

                <label for="team2">Team 2 Players (comma-separated):</label>
                <input type="text" name="team2Players" id="team2" class="form-control" required>

                <label for="additionalPlayers">Additional Players Needed:</label>
                <input type="number" name="addPlayers" id="additionalPlayers" class="form-control" required>
                <input type="hidden" name ="_csrf" value="<%= csrfToken %>">
                <label for="date">Date:</label>
                <input type="date" name="date" id="date" class="form-control" required>

                <label for="time">Time:</label>
                <input type="time" name="time" id="time" class="form-control" required>

                <label for="venue">Venue:</label>
                <input type="text" name="venue" id="venue" class="form-control" required>
  
      <button type="submit">Create Session</button>
    </form>
        </center>
    </div>
    </div>

    <!-- Bootstrap JS and Popper.js (required for Bootstrap components) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Add your script to set the username -->
</body>
</html>
